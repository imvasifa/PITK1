services:
  - type: web
    name: price-is-the-king
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app3:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.12.6
